import { DynoComponentDirective } from './dyno-component.directive';

describe('DynoComponentDirective', () => {
  it('should create an instance', () => {
    const directive = new DynoComponentDirective();
    expect(directive).toBeTruthy();
  });
});
